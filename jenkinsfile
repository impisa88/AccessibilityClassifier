pipeline{
    agent any

    stages {
        stage('Build') {
            steps {
                script{
                    dockerapp = ("impisa88/pipeline_test", '-f' ./dockerfile )
                }
            }
        }
    }
}
pipeline{
    agent any

    stages {
        stage('Build Image'){
            steps{
                script{
                    dockerapp = docker.build("pipeline_test", '-f ./dockerfile ./trabalhoMestrado')
                }
            }
        }
    }
}